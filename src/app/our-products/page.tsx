import Title from '@/reusableComponents/title/Title';
import React from 'react';

const page = () => {
    return (
        <div>
            <Title label={"Our Products"} titlePage={true}/>
        </div>
    );
};

export default page;